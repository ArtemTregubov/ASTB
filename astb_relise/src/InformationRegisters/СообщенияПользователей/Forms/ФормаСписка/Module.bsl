
&НаКлиенте
Процедура ОчиститьВсеСообщения(Команда)
	
	ОчиститьВсеСообщенияНаСервере();
	
	ЭтаФорма.ПодчиненныеЭлементы.Список.Обновить();
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОчиститьВсеСообщенияНаСервере()
	
	НаборЗаписей = РегистрыСведений.СообщенияПользователей.СоздатьНаборЗаписей();
	НаборЗаписей.Прочитать();
	НаборЗаписей.Очистить();
    НаборЗаписей.Записать();
	
КонецПроцедуры
