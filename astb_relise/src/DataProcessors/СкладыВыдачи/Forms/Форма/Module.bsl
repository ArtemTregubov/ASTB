
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Если Параметры.ПодразделениеСотрудник <> Неопределено Тогда
		ТаблицаСкладовВыдачи = ПроцедурыРаботыСНормамиСервер.ПолучитьТаблицуСкладовВыдачи(Параметры.ПодразделениеСотрудник);
		ЗначениеВРеквизитФормы(ТаблицаСкладовВыдачи,"СкладыВыдачи");
	Иначе
		Отказ = Истина;
	КонецЕсли;

КонецПроцедуры
