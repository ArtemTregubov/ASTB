//////////////////////////////////////////////////////////////////////////////////////
// Процедуры записи движений в регистр.

Процедура ОтразитьОстаткиНоменклатуры(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаОстаткиНоменклатуры;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ОстаткиНоменклатуры.Записывать = Истина;
	Движения.ОстаткиНоменклатуры.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьВыданныеСредстваЗащиты(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаВыданныеСредстваЗащиты;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ВыданныеСредстваЗащиты.Записывать = Истина;
	Движения.ВыданныеСредстваЗащиты.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьПотребностьВыдачиСИЗ(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаПотребностьВыдачиСИЗ;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ПотребностьВыдачиСИЗ.Записывать = Истина;
	Движения.ПотребностьВыдачиСИЗ.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьЗанятыеРабочиеМеста(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаЗанятыеРабочиеМеста;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ЗанятыеРабочиеМеста.Записывать = Истина;
	Движения.ЗанятыеРабочиеМеста.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьЗаказыПоставщику(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаЗаказыПоставщику;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ЗаказыПоставщику.Записывать = Истина;
	Движения.ЗаказыПоставщику.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьЗаявкиНаВыдачу(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаЗаявкиНаВыдачу;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ЗаявкиНаВыдачу.Записывать = Истина;
	Движения.ЗаявкиНаВыдачу.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьЗначенияНормОрганизации(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаЗначенияНормОрганизации;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ЗначенияНормОрганизации.Записывать = Истина;
	Движения.ЗначенияНормОрганизации.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьОтсутствиеНаРабочемМесте(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаОтсутствиеНаРабочемМесте;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ОтсутствиеНаРабочемМесте.Записывать = Истина;
	Движения.ОтсутствиеНаРабочемМесте.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьСредстваЗащитыВЧистке(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаСредстваЗащитыВЧистке;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.СредстваЗащитыВЧистке.Записывать = Истина;
	Движения.СредстваЗащитыВЧистке.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьУстановленныеНормыВыдачиСИЗ(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаУстановленныеНормыВыдачиСИЗ;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.УстановленныеНормыВыдачиСИЗ.Записывать = Истина;
	Движения.УстановленныеНормыВыдачиСИЗ.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьДоступныеУсловияРаботыСотрудника(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаДоступныеУсловияРаботыСотрудника;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ДоступныеУсловияРаботыСотрудника.Записывать = Истина;
	Движения.ДоступныеУсловияРаботыСотрудника.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьИсторияОбращенийНаСклад(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаИсторияОбращенийНаСклад;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ИсторияОбращенийНаСклад.Записывать = Истина;
	Движения.ИсторияОбращенийНаСклад.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьСоответствияНоменклатурыВыдачи(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаСоответствияНоменклатурыВыдачи;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.СоответствияНоменклатурыВыдачи.Записывать = Истина;
	Движения.СоответствияНоменклатурыВыдачи.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьШтрихкодыНоменклатурыДляУчетаЧистки(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаШтрихкодыНоменклатурыДляУчетаЧистки;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ШтрихкодыНоменклатурыДляУчетаЧистки.Записывать = Истина;
	Движения.ШтрихкодыНоменклатурыДляУчетаЧистки.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьКодыМаркировкиВыданныхСредствЗащиты(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаКодыМаркировкиВыданныхСредствЗащиты;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.КодыМаркировкиВыданныхСредствЗащиты.Записывать = Истина;
	Движения.КодыМаркировкиВыданныхСредствЗащиты.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьЦеныНоменклатуры(ДополнительныеСвойства, Движения, Отказ) Экспорт
	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаЦеныНоменклатуры;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ЦеныНоменклатуры.Записывать = Истина;
	Движения.ЦеныНоменклатуры.Загрузить(Таблица);
	
КонецПроцедуры
	
Процедура ОтразитьПериодыЗимы(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаПериодыЗимы;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ПериодыЗимы.Записывать = Истина;
	Движения.ПериодыЗимы.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьМВЗПодразделений(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаМВЗПодразделений;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.МВЗПодразделений.Записывать = Истина;
	Движения.МВЗПодразделений.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьОсновноеМестоРаботыСотрудника(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаОсновноеМестоРаботыСотрудника;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ОсновноеМестоРаботыСотрудника.Записывать = Истина;
	Движения.ОсновноеМестоРаботыСотрудника.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьСобственныеСИЗ(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаСобственныеСИЗ;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.СобственныеСИЗ.Записывать = Истина;
	Движения.СобственныеСИЗ.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьВыдачуПоПотребности(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаВыдачаПоПотребности;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ВыдачаПоПотребности.Записывать = Истина;
	Движения.ВыдачаПоПотребности.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьВыдачуПоВедомости(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаВыданныеСредстваЗащитыПоВедомости;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ВыданныеСредстваЗащитыПоВедомости.Записывать = Истина;
	Движения.ВыданныеСредстваЗащитыПоВедомости.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьУточненныеПричиныСписания(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаУточненныеПричиныСписания;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.УточненныеПричиныСписания.Записывать = Истина;
	Движения.УточненныеПричиныСписания.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьСрокиНоскиПоУпрощеннойВыдаче(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаСрокиНоскиПоУпрощеннойВыдаче;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.СрокиНоскиПоУпрощеннойВыдаче.Записывать = Истина;
	Движения.СрокиНоскиПоУпрощеннойВыдаче.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьПриказыНаИнструктажи(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаПриказыНаИнструктажи;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ПриказыНаИнструктажи.Записывать = Истина;
	Движения.ПриказыНаИнструктажи.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьВредныеИОпасныеФакторыИРаботыНаРабочихМестах(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаВредныеИОпасныеФакторыИРаботыНаРабочихМестах;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ВредныеИОпасныеФакторыИРаботыНаРабочихМестах.Записывать = Истина;
	Движения.ВредныеИОпасныеФакторыИРаботыНаРабочихМестах.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьПриказыНаПроведениеМедицинскихОсмотров(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаПриказыНаПроведениеМедицинскихОсмотров;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ПриказыНаПроведениеМедицинскихОсмотров.Записывать = Истина;
	Движения.ПриказыНаПроведениеМедицинскихОсмотров.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьМедицинскиеОсмотрыСотрудников(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаМедицинскиеОсмотрыСотрудников;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.МедицинскиеОсмотрыСотрудников.Записывать = Истина;
	Движения.МедицинскиеОсмотрыСотрудников.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьИнструктажиСотрудников(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаИнструктажиСотрудников;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ИнструктажиСотрудников.Записывать = Истина;
	Движения.ИнструктажиСотрудников.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьРезультатыПроверкиЗнанийСотрудников(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаРезультатыПроверкиЗнанийСотрудников;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.РезультатыПроверкиЗнанийСотрудников.Записывать = Истина;
	Движения.РезультатыПроверкиЗнанийСотрудников.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьПриказыНаПроведениеПроверкиЗнаний(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаПриказыНаПроведениеПроверкиЗнаний;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ПриказыНаПроведениеПроверкиЗнаний.Записывать = Истина;
	Движения.ПриказыНаПроведениеПроверкиЗнаний.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьСогласияНаИнформирование(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаСогласияНаИнформирование;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.СогласияНаИнформирование.Записывать = Истина;
	Движения.СогласияНаИнформирование.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьСредстваЗащитыНаХранении(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаСредстваЗащитыНаХранении;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.СредстваЗащитыНаХранении.Записывать = Истина;
	Движения.СредстваЗащитыНаХранении.Загрузить(Таблица);
	
КонецПроцедуры

//+++АСТБ_Горюшин_Алексей_17125
Процедура ОтразитьРеализованныеСИЗ(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаРеализованныеСИЗ;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.РеализованныеСИЗ.Записывать = Истина;
	Движения.РеализованныеСИЗ.Загрузить(Таблица);
	
КонецПроцедуры //---АСТБ_Горюшин_Алексей_17125

Процедура ОтразитьФККОНоменклатуры(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаФККОНоменклатуры;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ФККОНоменклатуры.Записывать = Истина;
	Движения.ФККОНоменклатуры.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьУтилизированныеСИЗ(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаУтилизированныеСИЗ;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.УтилизированныеСИЗ.Записывать = Истина;
	Движения.УтилизированныеСИЗ.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьЗаписиНаСклад(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаЗаписиНаСклад;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ЗаписиНаСклад.Записывать = Истина;
	Движения.ЗаписиНаСклад.Загрузить(Таблица);
	
КонецПроцедуры

//АсТБ_Alexey_71263_********************************************************************

Процедура ОтразитьМВЗДолжностей(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаМВЗДолжностей;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.МВЗДолжностей.Записывать = Истина;
	Движения.МВЗДолжностей.Загрузить(Таблица);
	
КонецПроцедуры

//АсТБ_Alexey_71263_********************************************************************

//АсТБ_Alexey_72252_********************************************************************

Процедура ОтразитьЦеныРемонта(ДополнительныеСвойства, Движения, Отказ) Экспорт
	
	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаЦеныРемонта;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.ЦеныРемонта.Записывать = Истина;
	Движения.ЦеныРемонта.Загрузить(Таблица);
	
КонецПроцедуры

Процедура ОтразитьРемонтСредствЗащиты(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаРемонтСредствЗащиты;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.РемонтСредствЗащиты.Записывать = Истина;
	Движения.РемонтСредствЗащиты.Загрузить(Таблица);
	
КонецПроцедуры

//АсТБ_Alexey_72252_********************************************************************

//АсТБ_Alexey_79639_********************************************************************

Процедура ОтразитьСредстваЗащитыКПолучению(ДополнительныеСвойства, Движения, Отказ) Экспорт

	Таблица = ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаСредстваЗащитыКПолучению;
	
	Если Отказ ИЛИ Таблица.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Движения.СредстваЗащитыКПолучению.Записывать = Истина;
	Движения.СредстваЗащитыКПолучению.Загрузить(Таблица);
	
КонецПроцедуры

//АсТБ_Alexey_79639_********************************************************************