#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ИнициализироватьДанныеДокумента(ДокументСсылка, ДополнительныеСвойства) Экспорт
	
	ТаблицыДляДвижений = ДополнительныеСвойства.ТаблицыДляДвижений;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ЦеновоеСоглашениеПоРемонтуВидыРемонта.Ссылка.Дата КАК Период,
	|	ЦеновоеСоглашениеПоРемонтуВидыРемонта.Ссылка.Организация,
	|	ЦеновоеСоглашениеПоРемонтуВидыРемонта.Ссылка.Контрагент,
	|	ЦеновоеСоглашениеПоРемонтуВидыРемонта.ВидРемонта,
	|	ЦеновоеСоглашениеПоРемонтуВидыРемонта.Цена
	|ИЗ
	|	Документ.ЦеновоеСоглашениеПоРемонту.ВидыРемонта КАК ЦеновоеСоглашениеПоРемонтуВидыРемонта
	|ГДЕ
	|	ЦеновоеСоглашениеПоРемонтуВидыРемонта.Ссылка = &Ссылка";
	
	Запрос.УстановитьПараметр("Ссылка",ДокументСсылка);
	
	ТаблицыДляДвижений.Вставить("ТаблицаЦеныРемонта", Запрос.Выполнить().Выгрузить());
		
КонецПроцедуры

#КонецЕсли